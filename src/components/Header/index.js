import React from "react";
import "./style.css";

const Header = props => (
    <div className="row">
        <div className="header" id="main-content">
            <h1 className="text-center">Click each hero without clicking the same one twice!</h1>
        </div>
    </div>

)
export default Header;